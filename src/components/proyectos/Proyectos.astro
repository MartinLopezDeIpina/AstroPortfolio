---
import Proyecto2 from "./Proyecto2.astro"
import H2 from "../H2.astro"

---

<div class="divProyectosGeneral">
    <div class="divProyectos">
        <div class="divTerminal">
            <h3>martin:~/<span>Proyectos</span>$</h3>
            <div class="text"></div>
        </div>
        <div class="listaProyectos">
            <Proyecto2 urlImagen="/img/ajedrez.jpeg" nombre="ProyectoA" descripcion="esta es una descripción del proyecto, tiene que ser un poco largapa que ocupe varias líneas pq sino pues pel y pues un poco más sabes"
            tecnologias={["java","javafx","mysql"]}/> 
            <Proyecto2 urlImagen="/img/ajedrez.jpeg" nombre="ProyectoA" descripcion="esta es una descripción del proyecto, tiene que ser un poco largapa que ocupe varias líneas pq sino pues pel y pues un poco más sabes"
            tecnologias={["java","javafx","mysql"]}/> 
            <Proyecto2 urlImagen="/img/ajedrez.jpeg" nombre="ProyectoA" descripcion="esta es una descripción del proyecto, tiene que ser un poco largapa que ocupe varias líneas pq sino pues pel y pues un poco más sabes"
            tecnologias={["java","javafx","mysql"]}/> 
            <Proyecto2 urlImagen="/img/ajedrez.jpeg" nombre="ProyectoA" descripcion="esta es una descripción del proyecto, tiene que ser un poco largapa que ocupe varias líneas pq sino pues pel y pues un poco más sabes"
            tecnologias={["java","javafx","mysql"]}/> 
        </div>
    </div>
</div>

<style>
    .listaProyectos{
        width:100%;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        gap: 300px;
    }
    span{
        color: var(--color-primary-100);
    }
    h3{
        font-size:42px;
        font-weight: bold;
        color: var(--color-tertiary-100);
    }
    .divProyectosGeneral{
        margin-top:14vh;
    }
    .divProyectos{
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
    }

    .divTerminal{
        width:100%;
        display: flex;
        align-items: center;
        margin-bottom: 75px;
        align-self: left;
        left:0;
        gap:10px;
    }

    .svgTerminal{
        width:555.57px;
        height:42.2px;
        margin-right: 10px;
    }


    .text { 
        position:relative;
    }

    .text::after {
        content: "";
        transform: translateY(-50%);
        position: absolute;
        top: 0;
        right: -15px;
        /* Remove display: inline-block if not required to be on the same line as text etc */
        display: inline-block;
        background-color: #606060;
        vertical-align: top;
        width: 10px;
        /* Set height to the line height of .text */
        height: 37.8px;
        /* 
        Animation paramaters:
        blink = animation-name, 
        1s = animation-duration, 
        step-end = animation-timing-function,
        infinite = animation-iteration-count
        */
        -webkit-animation: blink 1s step-end infinite;
        animation: blink 1s step-end infinite;
    }

    @-webkit-keyframes blink {
        0% { opacity: 1.0; }
        50% { opacity: 0.0; }
        100% { opacity: 1.0; }
    }

    @keyframes blink {
        0% { opacity: 1.0; }
        50% { opacity: 0.0; }
        100% { opacity: 1.0; }
    }
</style>
