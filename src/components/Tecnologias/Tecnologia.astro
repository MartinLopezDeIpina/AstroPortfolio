---

let nombre = Astro.props.nombre;

let url = `/svg/tech/${nombre}.svg`;
---
<img alt=`Logo de ${nombre}` src={url} title={nombre}>

<style>
    img {
        width: 60px;
        height: 60px;
    }

    img:hover {
        transform: scale(1.15);
        transition: all 0.2s ease;
    }

</style>

